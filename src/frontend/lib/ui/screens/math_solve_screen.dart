import 'package:flutter/material.dart';

class MathSolveScreen extends StatelessWidget {
  const MathSolveScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Math Assistant"),
      ),
      body: const SizedBox(),
    );
  }
}